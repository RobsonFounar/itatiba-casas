// Preencha com os dados do seu projeto Supabase (Settings → API).
// A chave "anon" é pública por desenho; a segurança fica nas políticas RLS.
// Nunca coloque a service_role key aqui.
//
// Depois de criar a conta, marque o admin no SQL Editor:
//   update public.profiles set role = 'admin' where email = 'seu@email.com';
window.ITATIBA_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",
};
